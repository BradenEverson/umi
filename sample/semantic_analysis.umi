fn foo(a: u8) -> i32 {
    return 10;
}

fn bar() -> u8 {
    return 10;
}

struct B {
    a: A,
}

struct A {
    thing: f32,
    another: bool,
    // problem: NotAType,
}


// fn baz(a: NotAType) -> void {}
// fn faz(a: u32) -> NotAType {}

fn main() -> void {
    let mut a: u32 = 10;

    // 10 + true;
    a = 9;

    let b: u8 = 100;

    // b = 10;
    a = 100;

    foo(1);
    bar();

    // Does not exist!
    // fas();
}
